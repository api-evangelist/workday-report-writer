naftiko: 1.0.0-alpha2
info:
  label: Workday Report Writer Workday Prism Analytics API — Data Change Tasks
  description: 'Workday Report Writer Workday Prism Analytics API — Data Change Tasks. 2 operations. Lead operation: Workday
    Report Writer List Data Change Tasks. Self-contained Naftiko capability covering one Workday Report Writer business surface.'
  tags:
  - Workday Report Writer
  - Data Change Tasks
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    WORKDAY_REPORT_WRITER_API_KEY: WORKDAY_REPORT_WRITER_API_KEY
capability:
  consumes:
  - type: http
    namespace: prism-analytics-data-change-tasks
    baseUri: https://{hostname}/api/prismAnalytics/v3/{tenant}
    description: Workday Report Writer Workday Prism Analytics API — Data Change Tasks business capability. Self-contained,
      no shared references.
    resources:
    - name: dataChangeTasks
      path: /dataChangeTasks
      operations:
      - name: listdatachangetasks
        method: GET
        description: Workday Report Writer List Data Change Tasks
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: dataChangeTasks-taskId
      path: /dataChangeTasks/{taskId}
      operations:
      - name: getdatachangetask
        method: GET
        description: Workday Report Writer Get a Data Change Task
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.WORKDAY_REPORT_WRITER_API_KEY}}'
  exposes:
  - type: rest
    namespace: prism-analytics-data-change-tasks-rest
    port: 8080
    description: REST adapter for Workday Report Writer Workday Prism Analytics API — Data Change Tasks. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/datachangetasks
      name: datachangetasks
      description: REST surface for dataChangeTasks.
      operations:
      - method: GET
        name: listdatachangetasks
        description: Workday Report Writer List Data Change Tasks
        call: prism-analytics-data-change-tasks.listdatachangetasks
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/datachangetasks/{taskid}
      name: datachangetasks-taskid
      description: REST surface for dataChangeTasks-taskId.
      operations:
      - method: GET
        name: getdatachangetask
        description: Workday Report Writer Get a Data Change Task
        call: prism-analytics-data-change-tasks.getdatachangetask
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: prism-analytics-data-change-tasks-mcp
    port: 9090
    transport: http
    description: MCP adapter for Workday Report Writer Workday Prism Analytics API — Data Change Tasks. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: workday-report-writer-list-data
      description: Workday Report Writer List Data Change Tasks
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: prism-analytics-data-change-tasks.listdatachangetasks
      outputParameters:
      - type: object
        mapping: $.
    - name: workday-report-writer-get-data
      description: Workday Report Writer Get a Data Change Task
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: prism-analytics-data-change-tasks.getdatachangetask
      outputParameters:
      - type: object
        mapping: $.
